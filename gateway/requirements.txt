# Athleia.ai — Enterprise API Gateway
# All service dependencies pinned to avoid drift

# ── Web Framework ──────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1

# ── Async HTTP Client (reverse proxy engine) ───────────────
httpx==0.28.1

# ── Configuration ──────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
pyyaml==6.0.2

# ── Structured Logging ─────────────────────────────────────
structlog==24.4.0

# ── Testing ────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1       # also used as async test client
respx==0.22.0       # mock httpx calls in tests
